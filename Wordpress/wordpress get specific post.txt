<?php                    $my_id = 4;                    $post_id_4 = get_post($my_id, ARRAY_A);                    $title = $post_id_4['post_excerpt'];?>                    <?php                                        // Change the excerpt length to 20 words                                        function custom_excerpt_length( $length ) {                        return 20;                    }                    add_filter( 'excerpt_length', 'custom_excerpt_length', 999 );                                        // Change the Read More to the button                                        function new_excerpt_more($more) {                           global $post;                        return '<a href="'. get_permalink($post->ID) . '" class="find-out-more-featured" >find out more</a>';                    }                    add_filter('excerpt_more', 'new_excerpt_more');                                        // Get the actual excerpt                                        the_excerpt();                                        ?>                                        <?php                    endforeach;                    ?>